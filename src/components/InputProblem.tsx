import { BaseProblemProps } from './ProblemSetBlock'

interface InputProblemProps extends BaseProblemProps {}

export const InputProblem = ({ solvedCallback, exhaustedCallback, allowedRetryCallback }: InputProblemProps): JSX.Element => {
  return <></>
}
