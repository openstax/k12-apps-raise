import { BaseProblemProps } from './ProblemSetBlock'

interface MultiSelectProps extends BaseProblemProps {}

export const MultiselectProblem = ({ solvedCallback, exhaustedCallback, allowedRetryCallback }: MultiSelectProps): JSX.Element => {
  return <></>
}
